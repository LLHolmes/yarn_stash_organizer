<div class="inside">
  <h1>Tools</h1>

<% if !tools.empty? %>
  <div class="list-tools">
    <% tools.each do |tool| %>
      <div class="each-tool">
        <%= link_to tool.name, edit_tool_path(tool) %> <%= list_project tool %>
      </div>
    <% end %>
  </div>
<% end %>
</div>
